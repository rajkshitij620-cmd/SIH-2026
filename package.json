{
  "name": "tourmitra-ai-workspace",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix frontend",
    "install:all": "npm install --prefix frontend && python3 -m pip install --user -r backend/requirements.txt",
    "dev": "concurrently -n backend,frontend -c green,cyan \"cd backend && ./.venv/bin/python -m uvicorn app.main:app --reload --port 8000\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "test": "cd backend && python3 -m pytest -q"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18"
  }
}
